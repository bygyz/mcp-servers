[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "trianova-mcp-centreon"
version = "0.1.0"
description = "MCP server for Centreon — AI-ready monitoring tools for IT ops agents"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["mcp", "centreon", "monitoring", "aiops", "trianova"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: System Administrators",
    "Topic :: System :: Monitoring",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
trianova-mcp-centreon = "trianova_mcp_centreon.server:main"

[project.urls]
Homepage = "https://github.com/trianova/mcp-servers"
Repository = "https://github.com/trianova/mcp-servers"
"Bug Tracker" = "https://github.com/trianova/mcp-servers/issues"
