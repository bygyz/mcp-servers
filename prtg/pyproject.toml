[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "trianova-mcp-prtg"
version = "0.2.0"
description = "MCP server for PRTG Network Monitor — AI-ready monitoring tools for IT ops agents"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["mcp", "prtg", "monitoring", "aiops", "trianova"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: System Administrators",
    "Topic :: System :: Monitoring",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
trianova-mcp-prtg = "trianova_mcp_prtg.server:main"

[project.urls]
Homepage = "https://github.com/trianova/mcp-servers"
Repository = "https://github.com/trianova/mcp-servers"
"Bug Tracker" = "https://github.com/trianova/mcp-servers/issues"
